{
  "include": ["src"],
  "exclude": [
    "node_modules",
    "**/*.test.*",
  ],
  "compilerOptions": {
    // to override config from tsconfig.base.json
    "outDir": "lib",
    "rootDir": "src",
    // for references
    "baseUrl": "src",
    "composite": true
  },
  // references required for monorepo to work
  "references": [{
    "path": "../pack-test-2"
  }]
}